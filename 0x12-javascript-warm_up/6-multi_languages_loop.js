#!/usr/bin/node
/* now with loops and arrays
*/
const stringArray = ['C is fun', 'Python is cool', 'Javascript is amazing'];
let i = 0;
for (i; i < stringArray.length; i++) {
  console.log(stringArray[i]);
}
